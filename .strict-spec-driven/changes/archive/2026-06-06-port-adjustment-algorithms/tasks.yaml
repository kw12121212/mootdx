schema: strict-spec-driven/task-list/v2
schema_version: 2
artifact_type: task_list
schema_file: templates/strict-spec-driven/schemas/task-list.yaml
change: port-adjustment-algorithms
sections:
  Implementation:
    implement-reversion:
      text: Port tools/reversion.ts with factorReversion, _reversion, etfReversion, baoliQfq, and reversion dispatcher. All operate on OhlcBar[] arrays.
      status: complete
    implement-adjust-utils:
      text: Port utils/adjust.ts with fqFactor (Sina Finance API), getXdXr (cached), toAdjust, toAdjust2 dispatchers.
      status: complete
    update-exports:
      text: Update mootdx-ts/src/index.ts to export adjustment functions.
      status: complete
  Testing:
    run-build:
      text: Run build validation.
      status: complete
      command: cd mootdx-ts && bun run build
    run-lint:
      text: Run lint checks.
      status: complete
      command: cd mootdx-ts && bun run lint
    run-tests:
      text: Run unit tests.
      status: complete
      command: cd mootdx-ts && bun test
  Verification:
    verify-scope:
      text: Verify implementation matches proposal scope.
      status: complete
testing_gates:
  validation_task: run-build
  unit_test_task: run-tests
