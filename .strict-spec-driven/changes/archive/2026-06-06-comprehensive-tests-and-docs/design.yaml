schema: strict-spec-driven/change-design/v1
schema_version: 1
artifact_type: change_design
schema_file: templates/strict-spec-driven/schemas/change-design.yaml
change: comprehensive-tests-and-docs
approach:
  - Add tests co-located with source files (*.test.ts). Focus on protocol helpers, binary parsers with fixture data, reversion algorithms, utils, and cache. Create README.md with API overview.
decisions:
  test-colocation:
    decision: Co-locate test files with source files instead of separate tests/ directory, matching existing pattern
    rationale: Existing tests already use co-located pattern. Bun test runner discovers .test.ts files automatically.
alternatives: []
risks: []
