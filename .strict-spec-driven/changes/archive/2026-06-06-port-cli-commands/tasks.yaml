schema: strict-spec-driven/task-list/v2
schema_version: 2
artifact_type: task_list
schema_file: templates/strict-spec-driven/schemas/task-list.yaml
change: port-cli-commands
sections:
  Implementation:
    implement-cli:
      text: Port CLI commands (quotes, reader, bestip, affair, bundle) to Commander.js in mootdx-ts/src/cli/index.ts.
      status: complete
    update-package-json:
      text: Add bin field to package.json for mootdx command.
      status: complete
    update-exports:
      text: Update index.ts exports if needed.
      status: complete
  Testing:
    run-build:
      text: Run build validation.
      status: complete
      command: cd mootdx-ts && bun run build
    run-lint:
      text: Run lint checks.
      status: complete
      command: cd mootdx-ts && bun run lint
    run-tests:
      text: Run unit tests.
      status: complete
      command: cd mootdx-ts && bun test
  Verification:
    verify-scope:
      text: Verify implementation matches proposal scope.
      status: complete
testing_gates:
  validation_task: run-build
  unit_test_task: run-tests
