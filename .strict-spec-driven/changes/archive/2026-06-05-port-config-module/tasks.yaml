schema: strict-spec-driven/task-list/v2
schema_version: 2
artifact_type: task_list
schema_file: templates/strict-spec-driven/schemas/task-list.yaml
change: port-config-module
sections:
  Implementation:
    create-config:
      text: Create mootdx-ts/src/config.ts with setup(), get(), set(), has(), clone(), update(), path() functions and default CONFIG object
      status: complete
    update-index:
      text: Update mootdx-ts/src/index.ts to re-export config module
      status: complete
  Testing:
    run-build:
      text: bun run build succeeds
      status: complete
      command: cd mootdx-ts && bun run build
    run-lint:
      text: bun run lint passes
      status: complete
      command: cd mootdx-ts && bun run lint
    run-tests:
      text: bun test passes with unit tests for config get/set/has/clone/update/setup
      status: complete
      command: cd mootdx-ts && bun test
  Verification:
    verify-scope:
      text: Verify only config.ts created and index.ts updated; Python codebase untouched
      status: complete
testing_gates:
  validation_task: run-build
  unit_test_task: run-tests
