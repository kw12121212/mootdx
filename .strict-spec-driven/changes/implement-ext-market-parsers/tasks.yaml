schema: strict-spec-driven/task-list/v2
schema_version: 2
artifact_type: task_list
schema_file: templates/strict-spec-driven/schemas/task-list.yaml
change: implement-ext-market-parsers
sections:
  Implementation:
    create-ext-parsers:
      text: "Create mootdx-ts/src/protocol/ext/ with setup, instruments, instrument_bars, instrument_quote parsers"
      status: complete
    update-index:
      text: "Update mootdx-ts/src/index.ts to re-export ext parsers"
      status: complete
  Testing:
    run-build:
      text: "bun run build succeeds"
      status: complete
      command: "cd mootdx-ts && bun run build"
    run-lint:
      text: "bun run lint passes"
      status: complete
      command: "cd mootdx-ts && bun run lint"
    run-tests:
      text: "bun test passes with ext parser tests"
      status: complete
      command: "cd mootdx-ts && bun test"
  Verification:
    verify-scope:
      text: "Verify ext/ directory created and index.ts updated"
      status: complete
testing_gates:
  validation_task: run-build
  unit_test_task: run-tests
